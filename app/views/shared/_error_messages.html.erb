<% if object.errors.any? %>
  <div class="alert alert-danger">
    <h4><%= pluralize(object.errors.count, "lỗi") %> đã xảy ra:</h4>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %> 